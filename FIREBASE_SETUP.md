# Firebase 设置指南

## 📋 第一步：创建 Firebase 项目

1. 访问 [Firebase Console](https://console.firebase.google.com/)
2. 点击"添加项目"或"Create a project"
3. 输入项目名称（如：psw-app）
4. 选择是否启用 Google Analytics（可选）
5. 点击"创建项目"

## 🔧 第二步：添加 Web 应用

1. 在 Firebase 项目控制台中，点击 **Web 图标** (</>) 
2. 输入应用昵称（如：PSW Password Manager）
3. **不需要**勾选"Firebase Hosting"
4. 点击"注册应用"
5. 复制显示的配置代码

## 📝 第三步：配置 Firebase SDK

复制 Firebase 配置信息，类似这样：

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456"
};
```

然后打开 `config/firebaseConfig.js` 文件，**替换** 配置信息：

```javascript
// 替换为你的 Firebase 配置
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",              // 替换这里
  authDomain: "YOUR_AUTH_DOMAIN",      // 替换这里
  projectId: "YOUR_PROJECT_ID",        // 替换这里
  storageBucket: "YOUR_STORAGE_BUCKET", // 替换这里
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID", // 替换这里
  appId: "YOUR_APP_ID"                 // 替换这里
};
```

## 🔐 第四步：启用 Firebase Authentication

1. 在 Firebase Console 左侧菜单，点击 **"Authentication"**
2. 点击 **"Get started"** 或 **"开始使用"**
3. 选择 **"Sign-in method"** 或 **"登录方法"** 标签
4. 启用 **"Email/Password"** 提供商：
   - 点击"Email/Password"
   - 启用第一个开关（Email/Password）
   - 点击"保存"

## 💾 第五步：设置 Firestore Database

1. 在 Firebase Console 左侧菜单，点击 **"Firestore Database"**
2. 点击 **"创建数据库"**
3. 选择 **"生产模式"** 或 **"测试模式"**
   - **测试模式**：30天内任何人都可以读写（仅用于开发）
   - **生产模式**：需要配置安全规则
4. 选择 Cloud Firestore 位置（建议选择 `asia-east1` 或 `asia-northeast1`）
5. 点击"启用"

## 🛡️ 第六步：配置 Firestore 安全规则（重要！）

在 Firestore Database 页面，点击"规则"标签，粘贴以下规则：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 密码集合规则：只允许用户访问自己的密码
    match /passwords/{passwordId} {
      allow read, write: if request.auth != null 
        && request.auth.uid == resource.data.userId;
      allow create: if request.auth != null 
        && request.auth.uid == request.resource.data.userId;
    }
  }
}
```

点击"发布"保存规则。

## 📦 第七步：安装依赖

在项目目录下运行：

```bash
npm install
```

或者如果依赖已经在 package.json 中，只需要运行安装命令。

## 🚀 第八步：运行项目

```bash
npm start
```

然后在 Expo Go 应用中扫描二维码即可测试！

## ✅ 功能说明

### 用户认证
- ✅ 注册新用户（邮箱 + 密码）
- ✅ 登录验证
- ✅ 退出登录

### 密码管理
- ✅ 添加密码（作用、账号、密码）
- ✅ 查看密码列表（仅显示当前用户的密码）
- ✅ 删除密码
- ✅ 下拉刷新

### 数据安全
- ✅ 每个用户只能访问自己的密码数据
- ✅ 使用 Firebase Authentication 进行身份验证
- ✅ Firestore 安全规则保护数据

## 🔍 测试流程

1. **注册新账号**：使用有效邮箱注册
2. **登录**：使用注册的邮箱和密码登录
3. **添加密码**：点击右下角 + 按钮添加密码
4. **查看密码**：在列表中查看已保存的密码
5. **删除密码**：点击密码卡片的"删除"按钮
6. **退出登录**：点击右上角"退出"按钮

## ❗ 常见问题

### 1. 登录失败
- 检查 Firebase 配置是否正确
- 确认 Email/Password 认证已启用
- 检查网络连接

### 2. 无法添加密码
- 确认 Firestore Database 已创建
- 检查安全规则是否正确配置
- 确认用户已登录

### 3. 看不到密码列表
- 下拉刷新尝试重新加载
- 检查控制台是否有错误信息
- 确认 Firestore 安全规则允许读取

## 📚 下一步优化建议

1. **密码加密**：使用加密算法保护密码
2. **密码显示/隐藏**：添加查看密码的开关
3. **编辑功能**：允许编辑已保存的密码
4. **搜索功能**：添加密码搜索
5. **分类管理**：按类别组织密码
6. **密码生成器**：自动生成强密码
7. **备份导出**：导出密码数据
8. **指纹/Face ID**：生物识别验证

---

需要帮助？检查 Firebase Console 的日志和调试信息！
